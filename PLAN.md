# TradeLab — 离线 K 线回放模拟交易 开发计划

> 本文档是后续持续开发的唯一依据。所有已锁定的设计决策、状态机规则、数据模型、界面与里程碑都在这里。改动任何已锁定决策必须先在文档里改，再改代码。

## 1. 产品定位（已锁定）

- **个人学习工具**：不是商业产品，不涉及上架、多用户、合规。
- **离线历史 K 线回放模拟器**：数据打包进 App，运行期完全不联网。
- **标的**：Binance 永续合约（USDT 本位）。
- **载体**：Web App（PWA）——桌面浏览器 / iOS Safari（Add to Home Screen）/ 安卓 Chrome 均可运行。不做原生 App，不付 iOS 上架费（$99/年开发者账号在本方案中不需要）。
- **核心哲学**：所有未明确定义的歧义，一律取**最差情况**（保守、真实、利于练习）。

## 2. 已锁定的设计决策（需求规格）

| 编号 | 决策 | 说明 |
|------|------|------|
| D01 | 时间轴语义 | 每个状态 = 某根 K 收盘瞬间；起始 K = 第 T 根（可见、只读），下单从 T 收盘后生效，T+1 起判定 |
| D02 | 步进 | 每次只推进 1 根 K；练习内**不可后退 / 快进 / 跳转**；有"重置回本练习起点" |
| D03 | 市价单成交 | 按**下一根 K 开盘价**成交 |
| D04 | 订单生效时机 | 所有订单从下单后**下一根 K 起**检测/成交 |
| D05 | 事件单 | 每根 K **至多触发一个**；优先级：强平爆仓 > 止损平仓 > 止盈平仓 > 开仓；**止损恒优先于止盈**（即使止盈单创建更早，止损也先触发，符合"最差情况"哲学）；触发后其余事件单**全部取消** |
| D06 | 强平 | 最高优先级；任何触及强平一律先爆，之后事件单不再执行 |
| D07 | 限价单 | **不属于事件单**；事件结算后再处理；可多笔成交，不受"每根一个"限制；不被事件单取消；持续挂单直到用户**单笔**取消 |
| D08 | 限价成交价 | 跳空越过（open 越过限价）按**开盘价**；触及按**限价** |
| D09 | 止损/突破单（Stop Order） | 触发价 = 下单那一刻那根 K 的 high/low（买/突破=high，卖/止损=low）；后续**每一根** K 判定穿越；一直挂到触发或取消 |
| D10 | Stop 成交价 | Buy-stop：open≥触发价则按 open，否则按触发价。Sell-stop：open≤触发价则按 open，否则按触发价 |
| D11 | 强平价公式 | Binance 官方（逐仓，含维持保证金率 MMR）：多 `avg×(1−(1−MMR)/L)`；空 `avg×(1+(1−MMR)/L)`。本期 MMR 用平值常量（默认 0.5%），分级 MMR 为未来增强 |
| D12 | 强平判定 | **逐根扫描**（每根 K 的 high/low 与强平价比对，多单看 low、空单看 high） |
| D13 | 保证金模式 | 仅**逐仓**（isolated）：每个仓位独立保证金，爆仓只亏该仓保证金，不影响余额 |
| D14 | 持仓方向 | **单向持仓**（one-way）：同交易对只允许一个净方向。多单中开空 → **拒单**（提示先平仓）；空单对称 |
| D15 | 加仓 / 部分平仓 | 加仓并入现有持仓并**重算均价与强平价**；部分平仓只减 qty、均价不变、按比例释放保证金；剩余 0 即平仓完成 |
| D16 | 平仓挂单关系 | 平仓操作**不影响**订单状态；部分平仓后挂单按剩余持仓 **clamp**；持仓清空后 reduce-only 挂单 **忽略**（不成交不报错） |
| D17 | 下单数量单位 | 开仓面板：数量格 + 单位下拉（币/USDT 名义），25/50/75/100% 快捷按钮 + 滑条按可用余额换算成保证金填入数量格；杠杆可调 |
| D17b | 平仓交互 | 持仓区独立平仓：一键市价全平 + 限价/止损平仓挂单（按持仓百分比），下单面板专注开仓 |
| D18 | 手续费 | 名义金额 × 固定费率，开平都收。taker（市价、Stop）= 高费（默认 0.05%）；maker（限价）= 低费（默认 0.02%） |
| D19 | 杠杆 | 下单时可选，per-position 记录 |
| D20 | 初始资金 | 练习前可配置，默认 1000 USDT |
| D21 | 频率 | 5m / 10m / 15m / 30m / 1h / 4h / 1d + 随机 |
| D22 | 品种 | 可选 / 随机 |
| D23 | 起始时间 | 可选 / 随机 / **开盘模式** |
| D24 | 开盘模式 | 仅支持 5m / 10m / 15m / 30m；随机选一个**工作日**，从**包含美股开盘时刻（09:30 America/New_York，含夏令时切换）**的那根 K 开始。时间显示用北京时间。本期不排除美股节假日（软点 S04） |
| D25 | 全随机 | 频率+品种+起始全随机；可选项"隐藏结果" |
| D26 | 隐藏 | 隐藏**品种名 + 日期**（时间不隐藏）；练习结束（用户结束或数据走完）时揭晓 |
| D27 | 背景历史 K | 起始点前显示 N 根（可配置，默认 200），**只读、不可交易** |
| D28 | 数据存储 | **只存 5m**；10/15/30/60/240/1440 从 5m 聚合（5 的整数倍，严格嵌套无损），练习开始时现算 |
| D29 | 1d 语义 | 边界 = **00:00 UTC**（Binance 官方定义）；显示时区为北京时间 |
| D30 | 续练 | 支持。同一 IndexedDB，无额外基础设施成本 |
| D31 | 报告 | 练习结束展示：盈亏金额 / 盈亏率 / 胜率 / 盈亏比 / 最大回撤 / 平均持仓时间（真实自然时间）/ 交易次数 / 成交记录；支持导出 **CSV**（报告 + 成交记录） |
| D32 | 隐藏持久化 | 隐藏状态必须随快照持久化，续练/重置不得绕过揭晓 |

## 3. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  React UI（setup / practice / report / library）        │
│  lightweight-charts 渲染 K 线                            │
├─────────────────────────────────────────────────────────┤
│  core/ —— 框架无关纯引擎（零第三方依赖）                 │
│  引擎接收"推进一根K" + "用户操作"，返回不可变新状态      │
│  整个状态 = 单一可序列化对象（见 §6.3）                 │
├─────────────────────────────────────────────────────────┤
│  data/ —— IndexedDB（idb 封装）                          │
│  5m K 线 / 导入的数据包 / 练习快照 / 设置               │
└─────────────────────────────────────────────────────────┘
      ▲ 开发机一次性导入（file picker），运行期零联网
scripts/download-binance-data.mjs  ──►  data/*.pack.json.gz
```

**关键约束**：核心引擎（core/）必须框架无关、纯函数式、可序列化。React 只是渲染层。这样续练 = `put(snapshot)` / `get(snapshot)`，且引擎可用 Vitest 无头测试。

## 4. 数据层

### 4.1 数据模型

```ts
interface Bar { t: number; o: number; h: number; l: number; c: number; v: number }
// t = 开柱时间 ms，UTC 对齐（5m 边界 = UTC 整 5 分钟）
// 只存 5m 一份；更高周期运行时聚合，不落库
```

### 4.2 聚合（D28/D29）

- 5m → 10/15/30/60/240/1440：严格分组（5 的整数倍），`open`=组首、`high`=max、`low`=min、`close`=组尾、`v`=sum。
- 1440 组 = 288 根 5m = 00:00 UTC 边界。
- 聚合成本：3 年 ≈ 31 万根 5m 聚合到 1d，毫秒级。

### 4.3 体积预算

- 单交易对 / 年 ≈ **5–10 MB**（紧凑数字数组）。
- 10 交易对 × 3 年 ≈ 150–300 MB；30 交易对 × 3 年 ≈ 450–900 MB。
- iOS Safari IndexedDB 单站点上限 ~1 GB，**目标压 500 MB 内**。个人练习建议 10–20 个交易对 × 2–3 年。

### 4.4 数据获取（首次启动自动拉取）

- **首次打开 App**：后台从 Binance 公共数据接口（`data.binance.vision`）自动下载默认 5 个品种（BTC/ETH/SOL/BNB/HYPE）最近 1 年 5m K 线，存 IndexedDB；带进度条 + 失败重试。
- 已有数据时跳过下载（不重复拉取）；下载完成后**彻底离线**。
- 浏览器内直接 fetch zip → 内置 ZIP 解析（EOCD + deflate-raw 解压）→ CSV 解析，无文件选择器。
- 保留开发脚本 `scripts/download-binance-data.mjs` 供预生成数据包（可选，非必需）。
- 品种目录：从已存数据集派生（symbol + 可交易时间范围）。

## 5. 核心引擎规范（状态机）

### 5.1 状态类型

```ts
interface PracticeState {
  version: number;
  sessionId: string;
  settings: PracticeSettings;      // D21–D27：tf / symbol / startMode / hidden / initialCapital / historyCount / leverage
  seed: number;                    // 随机可复现
  symbol: string;
  tfMs: number;
  startIndex: number;              // 起始 K = 第 T 根
  currentIndex: number;            // 当前展示到第几根
  historyCount: number;            // 背景历史 K 根数
  position: Position | null;
  orders: Order[];
  balance: number;                 // 已实现权益（可提取部分）
  initialCapital: number;
  equityCurve: number[];
  tradeRecords: TradeRecord[];     // 成交记录（含部分平仓子记录）
  hidden: { active: boolean; revealed: boolean };
  completed: boolean;
  stats: PracticeStats | null;     // 报告，结束时计算
}

interface Position {
  side: 'long' | 'short';
  qty: number;
  avgPrice: number;
  leverage: number;
  margin: number;                  // 已占用保证金
  liqPrice: number;                // D11
  realizedPnl: number;             // 该仓累计已实现盈亏（部分平仓累积）
  openedIndex: number;
}

interface Order {
  id: string;
  action: 'openLong' | 'openShort' | 'closeLong' | 'closeShort';  // 四种独立类型（D14 语义）
  orderType: 'market' | 'limit' | 'stop';
  qty: number;
  price?: number;                  // limit 限价 / stop 触发价
  triggerRef?: 'high' | 'low';     // stop 触发参考（下单那根 K），D09
  createdAtIndex: number;
  reduceOnly: boolean;
  status: 'active' | 'filled' | 'cancelled' | 'ignored';
  fillPrice?: number;
  fillIndex?: number;
  fee?: number;
}
```

### 5.2 每根 K 推进的确定性顺序

用户从第 T 根推进到第 T+1 根（`advance()`）：

**阶段 1 — 市价单成交**（按 `open(T+1)`）
- 按创建顺序逐笔成交；reduce-only 检查（无对应持仓 → ignored）。
- 平仓市价单成交后，该仓在此根的事件扫描以"已平仓"为起点。

**阶段 2 — 事件单结算**（每根至多触发一个，D05/D06）
1. **强平判断**：多单 `low(T+1) ≤ liqPrice`、空单 `high(T+1) ≥ liqPrice` → 强平。平仓、该仓保证金清零、余额结算、**全部事件单取消**。
2. 未强平则扫描事件单：买单/突破看 `high(T+1) ≥ 触发价`；卖单/止损看 `low(T+1) ≤ 触发价`。
3. 优先级：强平 > 止损平仓 > 止盈平仓 > 开仓。止损与止盈同根同时触发时**止损恒先**（不考虑创建时间）。仅当同一优先级层内**多个同类型单**（如两笔止损单）同时触发时，按**创建时间最早者**执行（确定性 tie-break）。
4. 该单按 D10 成交价成交，**其余事件单全部取消**。

**阶段 3 — 限价单结算**（D07/D08，可多笔）
- 逐笔按 D08 规则成交；reduce-only 无持仓 → ignored。

**阶段 4 — 更新状态**
- 重算持仓（均价/保证金/liqPrice）、balance、equity、资金曲线、成交记录。
- 数据走完（推进到最后一根）→ `completed = true`，进入报告（D31，含揭晓 D26）。

### 5.3 成交价约定（确定性，非撮合）

| 类型 | 成交价 |
|------|--------|
| 市价 | 下一根开盘价 |
| 限价买单 | open ≤ 限价 → open（跳空）；low ≤ 限价 ≤ high → 限价 |
| 限价卖单 | open ≥ 限价 → open（跳空）；low ≤ 限价 ≤ high → 限价 |
| Buy-stop | open ≥ 触发价 → open；否则触发价 |
| Sell-stop | open ≤ 触发价 → open；否则触发价 |

### 5.4 保证金 / 手续费 / 数量

- **可用** = `balance − position.margin`。
- **开仓所需保证金** = `qty × price / leverage`；下单数量单位见 D17（名义 BTC / 名义 USDT / 保证金 USDT / 保证金占可用比例）。
- **强平结算（逐仓）**：亏损吃掉整仓保证金，余额不额外扣。
- **手续费**：`qty × fillPrice × rate`；taker=0.05%、maker=0.02%（可配置常量）。
- **加仓**：`avg = (oldQty×oldAvg ± newQty×newPrice) / totalQty`，重算 liqPrice。
- **部分平仓**：qty 递减、avg 不变、按比例释放保证金、实现盈亏入 balance。

### 5.5 订单生命周期

`active → filled / cancelled / ignored`。挂单持续检测后续每根；未触及一直挂到用户单笔取消。平仓操作不改订单状态；部分平仓后挂单 clamp；持仓清空后 reduce-only 忽略。

### 5.6 时间轴 / 交互约束

- 起始 K = 第 T 根，可见但不可交易（D01/D27）。
- 推进仅"下一根"；提供"重置回起点"（回 T、清空持仓/挂单/成交/资金曲线、保留设置与隐藏状态）。
- 随机必须可复现：`seed` 存快照，同一 seed 复现同一局（续练/重置一致）。

## 6. 持久化与续练（D30/D32）

### 6.1 存储布局（IndexedDB）

```
datasets   : symbol → { bars: 5m Bar[], importedAt }     // 数据包导入
practices  : sessionId → PracticeState snapshot           // 练习快照（含未完成/已完成）
settings   : 全局默认设置
```

### 6.2 续练流程

- 快照存 `practices`；恢复时从 `datasets` 按 `symbol/tfMs` 重新聚合 bars（不内嵌 bars，体积小、一致）。
- 已完成练习保留快照用于报告查看/导出，直到用户删除。

### 6.3 设计约束（必守）

整个状态机必须是**单一可序列化对象**：所有字段为原始值或可 JSON 化嵌套对象，**禁止闭包、DOM 引用、运行时句柄**。任何破坏此约束的代码（如引擎内部缓存了非序列化句柄）都会让续练退化为重写。代码评审以此为准。

## 7. 界面（React）

| 屏幕 | 内容 |
|------|------|
| **主页 / Library** | 首次启动自动拉取数据（进度/重试）；品种清单；练习列表（续练、查看报告、删除）；新建练习 |
| **设置页 Setup** | 频率（含随机）、品种（含随机）、起始时间（含随机 / 开盘模式）、全随机 + 隐藏、初始资金、杠杆默认、背景 K 数 |
| **练习页 Practice** | K 线图（lightweight-charts：背景历史 + 已推进，随推进 append）；下单面板（方向×数量单位×数量×类型×价格/触发价×杠杆）；持仓面板（方向/qty/均价/保证金/强平价/浮动盈亏）；挂单面板（列表 + 单笔取消）；步进（下一根）/ 重置控制；品种/日期/时间显示（隐藏时打码，时间不隐藏） |
| **报告页 Report** | 指标（D31）+ 导出 CSV；隐藏时先揭晓再展示 |

## 8. 报告指标定义（D31，自然时间）

- 盈亏金额 = balance − initialCapital（含手续费）。
- 盈亏率 = 盈亏金额 / initialCapital。
- 胜率 = 盈利平仓笔数 / 总平仓笔数（盈亏>0 为胜；=0 不计胜负）。
- 盈亏比 = 平均盈利 / 平均亏损（绝对值）。
- 最大回撤 = 资金曲线峰谷最大跌幅（基于 equity 曲线）。
- 平均持仓时间 = 平仓时间 − 开仓时间，跨全部已平仓交易取均值，单位自然时间（ms 换算为 分/时/天）。
- 交易记录：一轮完整开→平为一条主记录；部分平仓拆为子记录并标注。

## 9. PWA / 离线

- `manifest.webmanifest` + service worker **全量预缓存**（静态资源全部打包，零运行时 fetch）。
- iOS：Add to Home Screen 安装；安卓 Chrome 自动提示安装。
- 体积红线：IndexedDB 占用 ≤ 500 MB（对应 10–20 交易对 × 2–3 年）。

## 10. 数据下载脚本（开发机）

- `scripts/download-binance-data.mjs`：配置品种列表 + 时间范围 → 下载 5m klines → 生成 `data/<symbol>.5m.json.gz` → 合并为单一数据包。
- 支持增量追加；数据包含 schema 版本字段。

## 11. 测试策略（Vitest）

| 模块 | 覆盖 |
|------|------|
| engine | 同根多事件冲突、强平优先、跳空越过、Stop 成交价约定、每根仅一事件、限价独立多笔、reduce-only、单向拒单、部分平仓 clamp、手续费 |
| aggregation | 5m→10/15/30/60/240/1440 的边界与 OHLCV 正确性 |
| market-open | 夏令时切换前后、工作日随机、边界对齐（北京时间显示） |
| stats | 胜率/盈亏比/最大回撤/平均持仓时间 |
| serialize | 快照 round-trip 无损、隐藏状态持久化 |

## 12. 开发阶段与里程碑

| 阶段 | 内容 | 完成标准 |
|------|------|----------|
| **P0** | 脚手架 + PWA 骨架（Vite+React+TS、manifest、sw） | 空应用离线可装、可跑 |
| **P1** | 数据层：IndexedDB、5m 存储、数据包导入、聚合、品种目录 | 导入→查询→聚合→展示单测过 |
| **P2** | 核心引擎：订单/执行/强平/保证金/手续费/单向持仓 | 引擎单测全绿 |
| **P3** | 设置页：频率/品种/起始/开盘/全随机/隐藏 | 可生成正确起始 index |
| **P4** | 练习页：图 + 下单 + 持仓 + 挂单 + 步进/重置 | 端到端走通一局 |
| **P5** | 报告 + CSV 导出 | 指标正确、导出可用 |
| **P6** | 续练：快照序列化/恢复 + Library 列表 | 中断→恢复状态一致 |
| **P7** | 数据下载脚本 + 生成真实数据包 | 导入真实 BTC/ETH 数据可回放 |
| **P8** | 打磨：桌面/iOS/安卓三端安装测试、体积检查 | 三端离线可用 |

## 13. 软点（需决策，不影响架构）

- **S01** MMR 平值 0.5% vs Binance 分级 MMR（本期平值，可配置常量）。
- **S02** 止损/止盈单是否允许非 reduce-only 用法（本期仅 reduce-only 语义）。
- **S03** 成交量 `v` 是否在 UI 展示（数据已存，UI 可选，D33 未锁）。
- **S04** 开盘模式是否排除美股节假日（本期仅排除周末）。

## 14. 未来增强（本期不做）

- 分级 MMR、资金费率、多标的持仓、历史回测（批量跳过）、快捷回放速度、音频/通知。
